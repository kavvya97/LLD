from package import Package
class NearestLockerStrategy:
    def determine_locker(package: Package):
        # based on nearest locker, this strategy assigns the locker
        # utilizes he package address and assigns the nearest locker
        return 0